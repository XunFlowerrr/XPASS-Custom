# Training method modules.
# source_only exposes:
#   setup(model, args, device) -> dict   # extra components (none for source-only)
#   trainer(src_dataloaders, model, optimizer, args, device, best_modelname, components)
